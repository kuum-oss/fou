global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'notifyhub'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['notifyhub-app:8080']
